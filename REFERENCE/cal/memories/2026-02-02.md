# Session Memory - 2026-02-02

## Last Session - Recents Feature Complete

**Working on:** Pixley Reader v1.1
**Status:** Recents feature implemented and documented

### Completed This Session

1. **Recents Feature** - Combined folders + files in StartView
   - `RecentFoldersManager.swift`: Added `removeFolderByPath()`, updated `removeRecentFile()` to match by path
   - `ContentView.swift`: Added `addRecentFile()` call when user clicks markdown file
   - `StartView.swift`: Renamed "Recent Folders" → "Recents", new `RecentItemButton` for both types
   - Shows up to 10 folders + 4 most recently clicked files, sorted by date

2. **Documentation Updated**
   - `cal/cal.md` - Added Recents feature session entry
   - `CLAUDE.md` - Updated service/view descriptions
   - `INDEX.md` - Updated status notes

### Previous Sessions (Same Day)

- Welcome folder bundled (6 markdown files in Resources/Welcome/)
- App icon set created
- AI Chat memory meter added
- Folder caching implemented
- Native UI refactor (generous hit targets, Pixelmator-style)
- Markdown count OOD fix (children bubble up to parents)

### Key Files Modified

```
Sources/
├── ContentView.swift          # Added addRecentFile() in handleTap()
├── Views/Screens/StartView.swift  # Recents UI with RecentItemButton
└── Services/RecentFoldersManager.swift  # Path-based removal methods
```

### Resume With

App is functional. Next steps from backlog:
- Test Welcome folder in TestFlight (may still need verification)
- Multi-platform exploration (iPad ~2-3hrs, iPhone ~3-4hrs, visionOS ~4-5hrs)
- v1.1 remaining stories (State Architecture, Native Sidebar)

### Build Note

SPM build shows FoundationModels errors (expected - macros need Xcode SDK). Build via Xcode for full functionality.
