import SwiftUI
import aimdRenderer

// MARK: - Gutter Constants

/// Constants for the gutter column in NativeDocumentView.
enum GutterView {
    /// Fixed gutter width (matches Plain mode's GutterOverlayView)
    static let width: CGFloat = 44
}
