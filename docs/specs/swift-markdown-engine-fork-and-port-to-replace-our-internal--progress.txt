swift-markdown-engine Fork & Port (Gate G4 revised) — Progress
==============================================================
Spec: docs/specs/swift-markdown-engine-fork-and-port-to-replace-our-internal-.md (v1.0, 2026-08-12)
Supersedes editor-epic.md § G4. Epic #107.

Phase P1: Fork & bind (kill box)            [COMPLETE — GO — 2026-08-12, 1 iteration]
  US-P1.1 DONE: forked to Applacat/swift-markdown-engine, vendored via
    git subtree (squashed) at Packages/swift-markdown-engine; remote
    'engine-fork' set. project.yml links MarkdownEngine product only
    (no CodeBlocks/Latex bridges — D-H). App builds; fork suite 323/323.
  US-P1.2 DONE: EngineStressHarness (--stress-engine, delegate-hook
    trigger like G2's) hosts NativeTextViewWrapper bound to
    MarkdownDocument via the P2-preview wiring (binding set closure gives
    (new, previous) → G3 fold-in → update → scheduleSave). Corpus moved
    to package main target (RoundTripCorpus.documents) shared by tests +
    harness. Corpus byte-fidelity: PASS in BOTH raw and live modes, all
    4 docs (storage == source bytes; binding untouched).
  US-P1.3 DONE: STRESS-ENGINE PASS — 500 keystrokes dropped=0
    selectionErrors=0; model sync; autosave ≤2.2s; undo reverts + model
    follows; D8 external-write undo no-op; G3 auto-merge both-survive;
    G3 clash-hold (save blocked during pill, keep-mine lifts);
    perf p95 11.89ms ≤ 16ms budget on 5,000-line doc (live mode).
  FORK PATCHES (both upstreamable, D-K):
    1. NativeTextViewWrapper.updateNSView: external programmatic
       replacement of the CURRENT document clears its undo stack
       (same rule as switch-back divergence) — D8.
    2. Coordinator observes NSUndoManagerDidUndo/RedoChange on vended
       managers and re-runs textDidChange: undo/redo replay does NOT
       post textDidChange, so bindings (and live styling) desync
       without it. Found by harness; their own binding consumers
       are exposed to this upstream.
  FINDINGS:
    - NSWindow harness windows need isReleasedWhenClosed=false (ARC
      double-release crash on close()).
    - Engine publishes binding a beat AFTER typing (async) — model-sync
      asserts must settle ~500ms; P2 wiring must not assume sync echo.
    - Block-scoped incremental restyle: a single giant paragraph
      (no blank lines, 258KB) costs ~460ms/keystroke — realistic docs
      are fine (11.9ms p95). Note for G6 soak: pathological-paragraph
      guard may be worth considering.
  Regression floor: app 320/320, package 175/175 (+16 swift-testing),
    STRESS-PLAIN PASS, fork 323/323.
Phase P2: Surface swap (full sweep)         [COMPLETE — 2026-08-12, 1 iteration]
  US-P2.1 DONE: MarkdownView rewritten — ONE NativeTextViewWrapper for both
    modes (Plain = rawSourceMode, Enhanced = live styling), flipped live by
    ViewModePicker via settings.behavior.interactiveMode. Binding routes
    (new, previous) through handleTextEdited (G3 fold-in intact) →
    MarkdownDocument → debounced autosave. Engine documentId = file path;
    wrapper NOT .id()-keyed so per-doc undo/scroll memory survives
    switches. Runtime AC in harness: live↔raw flip byte-exact both ways,
    undo dropped on flip (no-op after).
  US-P2.2 DONE: full sweep. App: MarkdownEditor, MarkdownEditorCoordinator,
    MarkdownHighlighter, InteractiveAnnotator, GutterOverlayView,
    MarkdownTextViewPopovers, MarkdownTextViewNavigation,
    LineNumberRulerView, Views/NativeRenderer/* (5 files),
    StressPlainHarness (retired — STRESS-ENGINE runs the same checks
    against the surviving surface). Package: legacy render pipeline
    (aimdRenderer.swift, DocumentModel, MarkdownAST, MarkdownBlock,
    MarkdownTheme, SwiftUITheme) + swift-markdown dependency RETIRED;
    SyntaxTheme.swift kept (palette catalog for P4 theming).
    MarkdownConfig extracted to its own file (AppCoordinator needs
    maxTextSize). AIMDReaderApp: ⌘]/⌘[ element navigation removed until
    P3; ⌘F now sets usesFindBar on the engine's text view (works on any
    NSTextView). SettingsRepository: highlighterScale removed.
    Grep gate CLEAN (one explanatory comment only).
  US-P2.3 DONE: G3 conflict UX runs against the engine surface — harness
    D8 external-replacement undo-clear, auto-merge both-survive, and
    clash-hold (save blocked during pill / keep-mine lifts) all PASS with
    the exact MarkdownView wiring. HUMAN SMOKE (non-blocking, P4 QA
    list): AI chat edit while typing lands without a reload pill.
  Accepted P2 regressions (restored in P4): gutter line numbers,
    bookmarks, comment indicators + Add Comment, reading-progress badge,
    ⌘G go-to-line, cross-launch scroll persistence (in-session per-doc
    scroll memory works via the engine).
  Floor: app 281/281 (was 320 — deleted suites of deleted code),
    package 153/153 + 7 swift-testing (legacy render tests removed),
    fork 323/323, STRESS-ENGINE PASS (incl. new mode-flip check,
    p95 10.12ms).
Phase P3: Interactive core four             [COMPLETE — 2026-08-17, 1 iteration]
  US-P3.1 DONE: generic interactive-element seam in the fork (data-driven,
    no host types). InteractiveElementSeam.swift: `.interactiveGlyph`
    (value InteractiveGlyph: symbolName/filled/opaque id — drawn SF Symbol
    control) + `.interactiveZone` (opaque String id — clickable text span).
    Engine does hit-testing (NativeTextView+InteractiveElement), glyph
    drawing (MarkdownTextLayoutFragment.drawInteractiveGlyphs), pointing-
    hand hover (CursorRects), mouseDown dispatch → onInteractiveElementClick
    (id, windowRect), and an overlay hook (onInteractiveOverlay) fired after
    BOTH restyle paths so the host re-asserts attrs the restyle cleared
    (raw mode skipped). Wrapper params + coordinator plumbing added.
    Fork tests: InteractiveElementSeamTests (4) — glyph hit, miss-outside,
    zone hit, glyph-beats-zone. Fork suite 327/327.
  US-P3.2 DONE: core four interactive. PixleyElementStyler (overlay) maps
    detector elements → seam attrs: choice options get a radio glyph
    (circle / circle.inset.filled) over the `[ ]` (source glyphs cleared);
    status label + fill-in get zones with chip / field styling. Checkbox
    stays engine-native. MarkdownViewInteractive (click router) parses the
    opaque id, re-detects by anchor, routes through InteractionHandler —
    the SAME serialized detector-anchored write path (status menu, fill-in
    popover). Harness US-P3.2: overlay lands glyph on choices + zones on
    status/fillin; byte-exact writes verified — selectChoice(0)→`[x] YES`,
    radio reselect moves the mark, advanceStatus preserves `**Status:**`,
    fillIn wraps `[[text: …]]` and re-detects.
    PERF LESSON: whole-doc detect per keystroke blew the budget (43ms) —
    the overlay now WINDOWS detection to the blank-line block enclosing the
    restyled range (elements outside keep their prior overlay). Back to
    11.45ms p95 / 16ms.
  US-P3.3 DONE (path): AI edits already route ChatTools → InteractionHandler
    (unchanged); the engine restyles + re-overlays the merged region via the
    same onInteractiveOverlay hook. Harness G3 auto-merge/clash scenarios
    (P1) stay green on this surface.
  US-P3.4 DONE: CriticMarkupExtensions — {++ ++}/{-- --}/{~~ ~~}/{== ==}/
    {>> <<} registered as engine InlineSyntax extensions, styled (distinct
    backgrounds, strikethrough/italic) but INERT (overlay never marks them
    glyph/zone → no hover cursor). Harness US-P3.4: addition has a
    background AND is not interactive. Corpus round-trip stays byte-exact
    WITH extensions registered (extensions are styling-only).
  HUMAN QA (P4 checklist): visually confirm the radio ring renders and a
    click selects; status menu + fill-in popover feel; hover cursors.
  Floor: app 281/281, package 153/153+7, fork 327/327, STRESS-ENGINE PASS
    (p95 11.45ms). Fork patches on branch pixley-fork.
  DOGFOOD POLISH (2026-08-18, post-loop): fill-ins now display cleanly —
    the `text:`/`date:`/`file:`/`folder:` storage prefix + `[[ ]]` collapse
    to clear ink, value carries the zone; date-typed fill-ins open a native
    NSDatePicker. Status chip gained a trailing chevron (new generic
    .interactiveAccessory fork seam) so it reads as a dropdown (D-F: no
    attachment). Dropped CriticMarkup substitution `{~~ ~> ~~}` — the `~>`
    aborts InlineSyntax's close scan; stays raw until G5.
  DEFERRED (confirmed with user 2026-08-18): review-block radios + click
    → G5 (not core-four); CriticMarkup accept/reject → G5; Add Comment /
    click-to-read-comment → P4 (this next phase).
Phase P4: Gutter parity & polish            [CODE COMPLETE — 2026-08-18, 1 iteration]
  US-P4.1 DONE: TextKit 2 line-number gutter (PixleyGutterRulerView,
    NSRulerView) enumerating textLayoutManager fragments (one per source
    line) for Y positions — the old ruler was TextKit 1 and couldn't run
    on the engine. Screenshot-verified (numbers 1..N aligned in a reserved
    left strip). Fork additions: config leftContentInset (the container
    reserves the gutter strip so text never overlaps — the scroll-view
    ruler reservation the custom container didn't honor) + onScrollViewReady
    hook. Harness gutter smoke: line numbers 1..12 monotonic.
  US-P4.2 DONE: bookmarks (dot + click-toggle via the ruler), comment
    indicators (<!-- feedback --> + CriticMarkup highlight, blue bubble),
    Add Comment via right-click → InteractionHandler.setGutterComment,
    reading-progress badge (scroll fraction), ⌘G go-to-line, cross-launch
    scroll persistence via engine onPersistScrollOffset/restoreScrollOffset
    → coordinator metadata (engine already restores in-session).
  US-P4.3 DONE: PixleyCodeHighlighter — internal SyntaxHighlighter adapter
    (strings/comments/numbers/shared keyword set across Swift/JS/Py/C),
    zero new deps (D-H), wired via services from the syntax-theme palette.
  US-P4.4 DONE: folded issues closed as superseded by the port — #81, #82,
    #83, #92, #94, #105 (old-renderer defects gone by construction), #84
    (scroll restore now engine + cross-launch), #93 (Add Comment via
    InteractionHandler), #104 (gutter alignment — new TextKit 2 gutter).
  Floor: app 281/281, package 153/153+7, fork 327/327, STRESS-ENGINE PASS.
  REMAINING (human): the 5-check manual QA list
    (docs/specs/ralph-renderer-identity-manual-test.md) re-run against the
    engine + a feel-check of the gutter/bookmarks/comments/date-picker —
    the one sign-off automation can't give. Fork patches on pixley-fork.

Regression floor at every phase exit: app suite green, aimdRenderer suite
green, vendored fork suite green, --stress-plain PASS (incl. D8 + G3 merge
+ clash-hold scenarios), restyle p95 <= 16ms/5000 lines from P1 on.

Kill box (P1 only): fundamental corpus/harness/perf failure needing > 1 week
of fork surgery => record NO-GO here, do NOT output the completion promise,
report to the user. Fallback: original editor-epic.md G4 plan.

Iteration log:
- (none yet)
