Editor Epic — Progress
======================
Spec: docs/specs/editor-epic.md (v1.0, 2026-07-23)

Gate G0: TextKit 2 spike (4-week kill box) [EXIT TESTS GREEN — 2026-07-23, day 1]
  US-0.1 PASS: 100× type-save-reload-undo with checkbox attachment; zero
    storage/model/disk divergence; caret traverses attachment atomically.
  US-0.2 PASS: Typora reveal on 5000-line/5000-run doc — reveal p95 0.30ms,
    unreveal p95 0.24ms, max 0.74ms (budget 16ms); doc byte-identical after
    200 cycles. Deviation: measured with ContinuousClock, not signposts.
  Architecture proven: storage = projection of source; bold markers mapped
    via custom attribute (removed from layout, reconstructed on serialize);
    reveal = local splice at caret entry, re-project on leave.
  Findings for G4:
    - TextKit 2 provider APIs not actor-annotated → spike runs Swift 5 mode;
      production needs a deliberate Swift 6 isolation pattern.
    - Attribute-mapped markers (not storage-mutation-with-offset-map) is the
      #91-safe pattern: serialize reconstructs from attributes.
  HUMAN FOLLOW-UP (before G1 starts):
    [ ] Jose: run the TextKit2Spike scheme interactively — feel the reveal,
        toggle the checkbox, type around it ("screen-recorded demo" AC
        satisfied by the live app + metrics; record if wanted).
    [ ] Jose: formal GO on TextKit 2 (kill criterion not triggered; this is
        the affirmative architecture decision for G4).
Gate G1: Document model + detector fixes   [COMPLETE — 2026-07-23]
  TextKit 2 GO recorded (Jose launched G1 past the G0 gate).
  US-1.1 DONE: MarkdownDocument + MarkdownDocumentRegistry (package,
    6 tests) and SaveCoordinator (app) exist; InteractionHandler's
    serializedWrite resolves the live model per URL and mutates it via
    SaveCoordinator (per-URL chain preserved as the save queue with
    FileWatcher settlement); ChatTools mutates the model via the same
    funnel; DocumentState.loadFile syncs disk truth into the registry.
    Zero behavior change: app 320/320, package 145/145.
  US-1.2 DONE: compute closures receive document.source — no disk read
    on the interaction path. Grep gate CLEAN (no Data(contentsOf:) in
    InteractionHandler/SaveCoordinator); SaveCoordinator logs
    "model-write (no disk read)" per mutation. Honest note: relocation
    still executes against the model (cheap, belt-and-braces); it fully
    demotes when views render FROM the model in G2+.
  US-1.3 DONE: detector fixes shipped + issues closed (#86 same-line
    choices, #87/#75 multi-word status, #88 typed-prefix filled values,
    #89 grammar + sanitization, #106 data-half — UI half stays G5).
  US-1.4 DONE: package suite green (#102 closed); swift test documented
    as required gate in CLAUDE.md.
Gate G2: Plain mode editable               [IN PROGRESS — iter 1 done 2026-07-23]
  US-2.1 DONE: display mutations deleted (padTableColumns, check glyphs);
    storage string == model string, asserted at the boundary. #91 dead —
    close it when the gate completes.
  US-2.2 CODE DONE: isEditable, substitutions off, attribute-only
    debounced re-highlight (string-equality guarded), lastAppliedText
    synced from typing. RUNTIME AC OUTSTANDING: scripted 500-char typing
    harness (add --stress-plain launch mode to the MAIN app, spike-style:
    programmatic typing incl. markdown syntax → no dropped keystrokes,
    selection stable, attributes correct).
  US-2.3 CODE DONE: model-routed edits + SaveCoordinator.scheduleSave
    (1.5s debounce) + ⌘S saveNow + undo cleared on external change (D8
    conservative interpretation: merge invalidates typed-undo stack).
    RUNTIME AC OUTSTANDING: harness verifies type→2s→disk matches, and
    simulated external write → undo does NOT restore pre-merge text.
  US-2.4 DONE: RoundTripCorpusTests in package (single-char toggle diff,
    reversible prose edits, pure detection, CRLF). Permanent merge gate.
  Suites: app 320/320, package 150/150. Perf note: refreshCommentedLines
    runs per keystroke (O(doc) regex scan) — acceptable now, flag for G4.
Gate G3: Conflict engine                   [PENDING]
Gate G4: Enhanced WYSIWYG surface          [PENDING]
Gate G5: Insert palette + polish           [PENDING]
Gate G6: Done bar / 2-week soak            [PENDING]

Iteration log:
- (none yet)
