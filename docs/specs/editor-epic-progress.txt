Editor Epic — Progress
======================
Spec: docs/specs/editor-epic.md (v1.0, 2026-07-23)

Gate G0: TextKit 2 spike (4-week kill box) [EXIT TESTS GREEN — 2026-07-23, day 1]
  US-0.1 PASS: 100× type-save-reload-undo with checkbox attachment; zero
    storage/model/disk divergence; caret traverses attachment atomically.
  US-0.2 PASS: Typora reveal on 5000-line/5000-run doc — reveal p95 0.30ms,
    unreveal p95 0.24ms, max 0.74ms (budget 16ms); doc byte-identical after
    200 cycles. Deviation: measured with ContinuousClock, not signposts.
  Architecture proven: storage = projection of source; bold markers mapped
    via custom attribute (removed from layout, reconstructed on serialize);
    reveal = local splice at caret entry, re-project on leave.
  Findings for G4:
    - TextKit 2 provider APIs not actor-annotated → spike runs Swift 5 mode;
      production needs a deliberate Swift 6 isolation pattern.
    - Attribute-mapped markers (not storage-mutation-with-offset-map) is the
      #91-safe pattern: serialize reconstructs from attributes.
  HUMAN FOLLOW-UP (before G1 starts):
    [ ] Jose: run the TextKit2Spike scheme interactively — feel the reveal,
        toggle the checkbox, type around it ("screen-recorded demo" AC
        satisfied by the live app + metrics; record if wanted).
    [ ] Jose: formal GO on TextKit 2 (kill criterion not triggered; this is
        the affirmative architecture decision for G4).
Gate G1: Document model + detector fixes   [IN PROGRESS — started 2026-07-23]
  TextKit 2 GO recorded (Jose launched G1 past the G0 gate).
  US-1.3 DONE: detector fixes shipped + issues closed (#86 same-line
    choices, #87/#75 multi-word status, #88 typed-prefix filled values,
    #89 grammar + sanitization, #106 data-half — UI half stays G5).
  US-1.4 DONE: package suite green 139/139 (#102 closed); swift test
    documented as required gate in CLAUDE.md. App suite 320/320.
  US-1.1 NEXT: MarkdownDocument + SaveCoordinator; reroute
    InteractionHandler's serializedWrite to read/mutate the model instead
    of disk (keep the per-URL chain as the save queue); ChatTools mutates
    the model. AC: zero behavior change, both suites green, no
    Data(contentsOf:) in interaction paths.
  US-1.2 AFTER: detector reads model; instrument toggle path to assert
    no disk read.
Gate G2: Plain mode editable               [PENDING]
Gate G3: Conflict engine                   [PENDING]
Gate G4: Enhanced WYSIWYG surface          [PENDING]
Gate G5: Insert palette + polish           [PENDING]
Gate G6: Done bar / 2-week soak            [PENDING]

Iteration log:
- (none yet)
