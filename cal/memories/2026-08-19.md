## SESSION — G4 engine port finished, G5 interactivity underway

**Working on:** v5 editor epic (#107) — swift-markdown-engine fork & port, then G5 interactive-element layer
**Branch:** main
**Uncommitted:** No (HEAD 93997a9)

### This Session
- **G4 (engine port) COMPLETE** — all four phases: P1 fork & bind (kill box GO), P2 surface swap (both modes on the engine; old editors/renderer/block-parser deleted, swift-markdown dep retired), P3 interactive core four (checkbox/choice/status/fill-in via a generic hit-test seam), P4 gutter parity (TextKit 2 line-number gutter, bookmarks, comments, ⌘G, progress badge, code highlighter). Fork lives at Applacat/swift-markdown-engine, vendored as a subtree at Packages/swift-markdown-engine, patches on branch pixley-fork.
- **Dogfood fixes:** fill-in clean display (hide `text:`/`date:` prefix) + native calendar date picker; status chevron; gutter rewritten as a floating overlay + re-parent (the real-app bug); gutter number aligns to the first visual line; choice "+" to add options (on their own line); Insert menu (menu bar + window-chrome toolbar) with ⌥⌘ shortcuts (#109 closed); review blocks → radio groups with notes popover (#111 closed).
- **Tickets filed:** #108 (recents delete-instead-of-open), #110 (selection popup Add Comment), #111 (review — DONE), #112 (CriticMarkup accept/reject + substitution styling), #113 (feedback), #114 (spec-4 rich controls), #115 (status native dropdown). Closed superseded #103/#106 and #81–#84/#92–#94/#104/#105.

### Resume With
- **G5 next units (one ralph each):** #113 feedback → #112 CriticMarkup (reuse the seam) → then #114 spec-4 controls + #115 status **after the attachment decision** → #110 selection popup (needs a fork `onSelectionChange` hook) → US-5.3 docs/smoke test → G6 soak.
- **Open user decision:** hover-highlight vs full Pop-style pointer-reactive sheen on interactive elements (I recommended hover-highlight now, sheen as a stretch ticket).
- **Open architecture decision (gates #114/#115):** attachments (NSTextAttachment view providers) vs styled-text hit-test for rich controls. Recommendation: hybrid — attachments for slider/stepper/toggle/color/status, styled text for checkbox/choice/fill-in/CriticMarkup.

### Hot Context
- **Verify UI in the REAL app, not just the harness.** The plain-window `--stress-engine` harness does NOT reproduce the browser's SwiftUI NavigationSplitView. The gutter passed in the harness but was invisible in the app. Use `--open-sample [--insert-demo] [--shot]` launched via `open` (kill the Xcode-held instance first: `kill -9 <pid>`; launching the binary directly creates no SwiftUI windows).
- **`onScrollViewReady` fires twice** — SwiftUI builds a throwaway 0×0 NSView then the live one. Don't guard-once onto the first; re-parent onto the current scroll view and force-refresh after layout.
- **os_log `.debug` isn't persisted** — use file-based diagnostics (app can write its container tmp: `~/Library/Containers/com.aimd.reader/Data/tmp/`).
- **The interactive-element seam** (fork): `.interactiveGlyph` (drawn control), `.interactiveZone` (clickable text), `.interactiveAccessory` (trailing symbol, decorative or clickable via identifier). Pixley overlay (`PixleyElementStyler`) maps detector elements → attributes; `MarkdownViewInteractive` routes clicks → `InteractionHandler`. Adding a new element = overlay case + router case + a harness check (+ `--shot`).
- Verification: app suite (xcodebuild test, 281), package (`cd Packages/aimdRenderer && swift test`, 153+7), fork (`cd Packages/swift-markdown-engine && swift test`, 327), `--stress-engine` harness. Progress tracked in docs/specs/g5-progress.txt.
